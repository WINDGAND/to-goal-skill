interface:
  display_name: "To Goal"
  short_description: "Orchestrate multiple skills toward a confirmed goal"
policy:
  # Meta-orchestrator: prefer explicit invoke to avoid hijacking single-skill tasks
  allow_implicit_invocation: false
